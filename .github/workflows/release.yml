name: Release

# Releases are cut by pushing a tag:
#
#   npm version minor        # updates package.json and creates the tag
#   git push --follow-tags
#
# This workflow verifies the tag, publishes to npm with provenance, and creates
# the matching GitHub release.

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Verify before publishing
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: npm
      - run: npm ci
      - run: npm run verify
      - run: npm run build

  publish:
    name: Publish to npm
    needs: verify
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      # Required for npm provenance, which lets consumers verify the published
      # package was built from this repository at this commit.
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: npm
          registry-url: https://registry.npmjs.org

      # Trusted publishing needs a recent npm. The version bundled with Node is
      # not guaranteed to be new enough, and the failure mode is an
      # authentication error rather than anything that names the real cause.
      - name: Use an npm that supports trusted publishing
        run: npm install -g npm@latest

      - run: npm ci

      - name: Check the tag matches the manifest version
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME#v}"
          manifest="$(node -p "require('./package.json').version")"
          if [ "$tag" != "$manifest" ]; then
            echo "Tag $GITHUB_REF_NAME does not match package.json version $manifest." >&2
            echo "Publishing would produce a release whose tag lies about its contents." >&2
            exit 1
          fi
          echo "Tag and manifest agree on $manifest."

      - run: npm run build

      # No token. Authentication is OIDC via trusted publishing, configured on
      # the package at npmjs.com against this repository and this workflow file.
      #
      # This deliberately replaces a granular token with bypass-2FA enabled,
      # which npm is in the process of restricting. Provenance is still signed,
      # because the publish still runs from GitHub Actions with id-token: write.
      - name: Publish
        run: npm publish --provenance --access public

      - name: Create the GitHub release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
